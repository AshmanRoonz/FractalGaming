name = "lss-turn"
main = "worker.js"
compatibility_date = "2026-01-01"

# Optional: lock CORS to your game's origin. Leave commented to allow any
# origin (fine for testing). Secrets TURN_TOKEN_ID / TURN_API_TOKEN are set
# with `wrangler secret put`, NOT here (never commit secrets).
# [vars]
# ALLOWED_ORIGIN = "https://lss.fractalreality.ca"
