--!strict
-- DISABLED: ambient pets moved server-side (see Battle.luau) so that in co-op every
-- player sees the same wild pets in the same spots. Kept as a no-op stub so nothing
-- breaks if it's still referenced; safe to delete.

local AmbientPetsController = {}

function AmbientPetsController.start() end

return AmbientPetsController
