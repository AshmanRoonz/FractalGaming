[
    {
        "angle":  "C1 â€” Codegen S-expression tree, per-genome WGSL JIT + pipeline foundry",
        "buildability":  5,
        "trueMetaGen":  9,
        "variety":  8,
        "performance":  4,
        "elegance":  6,
        "total":  6.4,
        "note":  "Genuinely new-KINDS story is strong (topology mutates via post-order codegen; interpret-head swap = \u0027one field read differently\u0027 as a literal tree edit). But the whole angle is built AROUND a compile budget it then has to fight: 64 distinct render pipelines, async createRenderPipelineAsync, LRU pool, B=2/frame foundry, subtree-dedupe â€” this is a LOT of moving machinery to cram into one HTML file, and a single codegen-escaping bug fails createShaderModule for that specimen. WGSL string-emit with scalar/vec2 coercion helpers is bug-prone. Perf per-pixel is great once compiled (fully inlined = hand-authored heightAt speed), but the \u0027developing shimmer\u0027 + ~1s populate on reseed and 64 live pipelines is exactly the compile-storm risk the prior art says is FATAL if mis-handled. Elegance suffers: it\u0027s the opposite of \u0027generator = data\u0027. Highest ceiling, lowest floor."
    },
    {
        "angle":  "C2 â€” Bytecode genome, ONE Field-VM interpreter, vec4 carrier + storage buffer",
        "buildability":  9,
        "trueMetaGen":  8,
        "variety":  8,
        "performance":  9,
        "elegance":  9,
        "total":  8.6,
        "note":  "The spine the prior art explicitly names as THE architecture: compile one interpreter, generators-as-data, mutate = writeBuffer, zero recompiles. Drops into the labs verbatim (FIELD/PARAMS prelude in scope, layout:\u0027auto\u0027, storage buffer, shader() helper). Uniform full-length MAX_OPS loop + vec4 universal-carrier + total NaN-scrubbed ops = every genome VALID and non-divergent by construction â€” matches every pitfall mitigation. trueMetaGen is real (CPU emitter grows the DAG topology + picks root interpret kind, not just k0..k2), slightly below C1/C3 only because a flat 48/128-node register-file DAG is a hair less expressive than C1\u0027s arbitrary tree or C3\u0027s added L-system. The honest gap it names itself (GRAD2/CURL2/DOWNHILL are secretly recursive; ITERATE family excluded) is handled cleanly (finite-diff of INPUT-noise only / separate fixed-step pass). Best buildabilityÃ—perfÃ—elegance product; the safe, correct backbone."
    },
    {
        "angle":  "C3 â€” Two-layer CPPN field (WHOLE) + bracketed L-system PART, one VM, NEAT+novelty",
        "buildability":  6,
        "trueMetaGen":  9,
        "variety":  9,
        "performance":  7,
        "elegance":  8,
        "total":  7.6,
        "note":  "Richest and best-fit-to-thesis: L1 CPPN = the WHOLE, L2 CPU-expanded L-system turtleâ†’SDF/particle instances = the PART placed by sampling L1, meta-selector = the CENTER. This is the only candidate that mechanically closes the three-ones AND reaches branching phenomena (creatures/coral/rivers/dendrites) that a coord-only field cannot make. Still one compiled VM (no storm) and same drop-in prelude, so perf is fine (docked for a second render path â€” instanced SDF impostors â€” plus a 48-reg vec4 file Ã— 16 tiles Ã— cloud raymarch getting heavy). Buildability is the cost: it\u0027s two genome layers, a CPU L-system expander with depth/MAX_SEG caps, an instanced impostor pipeline, AND the full MAP-Elites+novelty+union-find water-connectivity fitness â€” a lot for one file. trueMetaGen ties C1 for top. The L-system is the single most valuable idea no other candidate has."
    }
]
