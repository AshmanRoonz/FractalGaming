Ship C2's single-interpreter bytecode VM as the load-bearing backbone, then bolt on the two ideas C2 lacks and C3/C1 have: (A) C3's CPU-expanded bracketed L-system as an OPTIONAL second stage for branching PART phenomena, and (B) a real MAP-Elites + edge-of-chaos + novelty fitness filter (shared by C2 and C3) so the gallery is diverse-and-coherent, not a monoculture or mush.\n\nCore decisions:\n1. ONE compiled 'Field VM' WGSL module (never per-genome codegen â€” this kills C1's compile-storm risk outright). A generator is a fixed-capacity bytecode slice in a STORAGE buffer; mutate/breed/reseed = device.queue.writeBuffer only. This is the prior art's named spine and matches field_particle_join's storage-buffer + layout:'auto' + shader() conventions verbatim.\n2. Universal vec4 carrier + TOTAL, NaN-scrubbed, range-safe ops => every random genome is VALID and every wire type-checks (Sims coercion). Uniform full-length MAX_OPS loop with NOP masking => no workgroup divergence, no uniformity-analysis break. Topologically-sorted DAG in emit order => no traversal stack, no recursion (WGSL-legal).\n3. GENOME = C2's L1 field DAG (the WHOLE) PLUS C3's L2 L-system genome (the PART), both pure data in the same buffer. L2 is expanded on the CPU to a bounded turtleâ†’segment/stamp instance list, drawn instanced (reusing the labs' draw(6,nInstances) debris pattern) and colored by sampling L1 at each base point. This is the only way to reach creatures/coral/dendritic structure while staying non-recursive on GPU.\n4. The root interpret opcode fixes the KIND (height|density|color|flow|emissive) and routes to a render head â€” 'one field read differently' is one opcode. Different roots feeding different heads = categorically different generators, not re-rolled params.\n5. ALL evolution + selection on the CPU with a seeded integer PCG (mirrors hashU/rnd): NEAT-style complexify (add-node/add-conn/change-op/perturb) + innovation-id crossover + Sims mutation catalog. GPU is used only for (a) final field evaluation and (b) cheap 32Ã—32 descriptor render-to-texture whose readback is QUANTIZED before CPU integer compare â€” so two machines pick the same 64 winners from the same seed (resolves the labs' GPU-float-drift note). Fitness = edge-of-chaos gate (reject variance<eps frozen, reject flat/white spectrum via low/high band-energy ratio) + MAP-Elites archive over 3 cheap descriptors (roughness=mean|grad|, relief-entropy, water-connectivity) + novelty k-NN reject of near-duplicates. This guarantees 'novel, coherent, diverse'.\n6. Gallery = one 8Ã—8 (default) scissor/viewport grid rendered every frame from the one bytecode buffer, one pipeline set, per-tile genome index in a small uniform. Click a tile â†’ fullscreen free-fly world whose field feeds the EXISTING fieldâ†”particle scatter/gather+heal join (the invented heightAt drop-in-replaces the analytic one). Reseed/mutate/breed/pin all edit only buffers.\n\nThis merges C2 (perf + buildability + one-file cleanliness), C3 (three-ones fit + L-system branching richness + QD fitness), and C1's single genuinely-good idea (topology mutation = new kinds) â€” while discarding C1's per-genome shader JIT entirely.
